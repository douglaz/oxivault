[package]
name = "nrf52840-demo"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "nrf52840-demo"
path = "src/main.rs"

[dependencies]
oxivault-core = { path = "../../oxivault-core", default-features = false }
oxivault-embedded = { path = "../../oxivault-embedded", features = ["nrf52840"] }

embassy-executor = { version = "0.9", default-features = false, features = ["arch-cortex-m", "executor-thread"] }
embassy-time = { version = "0.3", default-features = false }
embassy-nrf = { version = "0.7", default-features = false, features = ["nrf52840", "time-driver-rtc1", "gpiote"] }

cortex-m = { version = "0.7" }
cortex-m-rt = { version = "0.7" }
defmt = "1.0"
defmt-rtt = "1.0"
panic-probe = { version = "1.0", features = ["print-defmt"] }

heapless = "0.8"

[profile.release]
debug = 2
lto = true
opt-level = "z"